def myinput():
  fname=input("Enter file:")
  return fname
def compute(fname):
  f=open(fname)
  count={}  
  w2=dict()  
  for line in f
    if line.startswith("From")
      email=line.split()[1]
      count[email]=count.get(email,0)+1
  min=0
  for email in count
  if min>count[email]
    email=mi
  if max<count[email]
    email=max

   
        
        